<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no" />
  <title>PoupMais NodeJs</title>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Libre+Baskerville:wght@400;700&family=Poppins:wght@100;300;400;500;600;700&family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="./css/global.css" />
  <link rel="stylesheet" href="./css/tipografia.css" />
</head>

<body>
{{#if loginOn}}
  {{{loginOn}}}
{{else}}
  <header class="header">
    <nav class="perfil">
      <ul class="perfil-ul">
        <img class="img-perfil" src="./img/Movimentacoes/account_circle_FILL0_wght300_GRAD0_opsz48.svg" />
        <li>
          {{#if userInfos}}
         <span class="font-p-m nome-login"> Olá, {{userInfos.name}} <span class="sobrenome">{{userInfos.surname}}</span></span>
          {{else}}
          <span class="font-p-m nome-login"></span>
          {{/if}}
        </li>
      </ul>
      <li class="links">
        <a class="inicial" href="./movimentacoes">INICIAL</a>
        <a class="atividades" href="./transacoes">ATIVIDADES</a>
        <a href="/logout">
         <span class="sair">SAIR<img class="setinha" src="./img/Movimentacoes/Arrow Fowards.svg" alt="seta"></span></a>
      </li>
    </nav>
  </header>
{{/if}}
<body>
    {{#if messages.message}}
      <span class="msg">{{messages.message}}</span>
    {{/if}}
  {{{body}}}
</body>
<script src="./script/movimentacoes.js"></script>
<script src="./script/ativarForm.js"></script>

</html>